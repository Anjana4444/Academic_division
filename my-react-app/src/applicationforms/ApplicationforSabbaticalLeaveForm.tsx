import styles from './AgreementForm.module.css';

export default function ApplicationforSabbaticalLeaveForm() {
  return (
    <div className={styles.page}>
      {/* Hero Section - The dark area with dots */}
      <section className={styles.hero}>
        <div className={styles.heroContent}>
          <h1 className={styles.title}>Sabbatical Leave </h1>
          <div className={styles.redUnderline}></div>
        </div>
      </section>

      

     
    </div>
  );
}